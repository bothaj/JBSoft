using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace JBSoft.Dal.Models
{
    /// <summary>
    /// Enum representing the different states that the data can be in
    /// </summary>
    public enum ActiveStatus
    {
        Deleted = 0,

        Active = 1
    }
}
